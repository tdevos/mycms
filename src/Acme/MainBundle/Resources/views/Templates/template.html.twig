{% extends "AcmeMainBundle::layout.html.twig" %}

{% block content %}

    <div class="jumbotron">
        {% createphp cmfMainContent as="rdf" %}
        {{ rdf.title|raw }}
        {{ rdf.body|raw }}
        {% endcreatephp %}
    </div>

    {{ sonata_block_render({ 'name': 'firstBlock' }, {
    'template': 'AcmeMainBundle:Blocks:block.html.twig'
    })}}

    {{ sonata_block_render({ 'name': 'secondBlock' }, {
    'template': 'AcmeMainBundle:Blocks:block.html.twig'
    })}}

    {{ sonata_block_render({ 'name': 'thirdBlock' }, {
    'template': 'AcmeMainBundle:Blocks:block.html.twig'
    })}}

    {#{{ sonata_block_render({ 'name': 'additionalInfoBlock' }, {#}
    {#'divisible_by': 3,#}
    {#'divisible_class': 'row',#}
    {#'child_class': 'col-md-4'#}
    {#}) }}#}

{% endblock %}

